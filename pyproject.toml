[tool.poetry]
name = "textovka"
version = "0.1.0"
description = "Indiana Jones 2 remake written in Python."
authors = ["Miroslav Biňas <miroslav.binas@tuke.sk>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
